<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ICE_FILE" class="ch.qos.logback.core.FileAppender">
        <file>webrtc-test.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="TEST_FILE" class="ch.qos.logback.core.FileAppender">
        <file>integration-test.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="DEBUG">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="TEST_FILE" />
    </root>

    <logger name="dev.getelements.elements.crossfire.client.webrtc" level="DEBUG" additivity="false">
        <appender-ref ref="ICE_FILE" />
    </logger>

</configuration>
